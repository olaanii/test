void main() {
  // var city = "Adama";  -> this only used when we don't need to return anything
  //                         not to change its data type when using it

  dynamic city = "Adama";
  city = 10;

  print(city);
}
